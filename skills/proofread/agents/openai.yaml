interface:
  display_name: "Proofread"
  short_description: "Conservative proofreading with protected diffs"
  default_prompt: "Use $proofread to correct and polish this text without changing its meaning or voice."
policy:
  allow_implicit_invocation: true
